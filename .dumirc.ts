import { defineConfig } from 'dumi';

export default defineConfig({
  favicons: ['/logo.ico'],
  themeConfig: {
    logo: "/logo.png",
    footer: false,
    socialLinks: {
      github: 'https://github.com/JayYuen666',
    },
  },
});
